<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
        android:layout_height="@dimen/nav_header_height"
        android:background="@color/colorPrimary"
        android:theme="@style/ThemeOverlay.AppCompat.Dark"
        android:gravity="bottom" android:id="@+id/linearLayout">

    <ImageView
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:paddingTop="@dimen/nav_header_vertical_spacing"
            app:srcCompat="@drawable/profiledefault"
            android:contentDescription="@string/nav_header_desc"
            android:id="@+id/imgUserNavHeader"
            android:layout_marginStart="8dp"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginBottom="8dp" app:layout_constraintBottom_toTopOf="@+id/btnLoginNavHeader"/>

    <TextView
            android:layout_width="wrap_content"
            android:layout_height="31dp"
            android:paddingTop="@dimen/nav_header_vertical_spacing"
            android:textAppearance="@style/TextAppearance.AppCompat.Body1"
            android:id="@+id/txtNameNavHeader"
            app:layout_constraintStart_toEndOf="@+id/imgUserNavHeader"
            android:layout_marginStart="8dp"
            app:layout_constraintTop_toTopOf="@+id/imgUserNavHeader"/>

    <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/txtEmailNavHeader"
            app:layout_constraintStart_toEndOf="@+id/imgUserNavHeader" android:layout_marginStart="8dp"
            app:layout_constraintTop_toBottomOf="@+id/txtNameNavHeader"/>
    <ImageButton
            android:layout_width="30dp"
            android:layout_height="30dp" app:srcCompat="@drawable/addchannelbutton"
            android:id="@+id/btnAddChannel"
            android:scaleType="fitXY" android:background="@android:color/transparent"
            android:padding="5dp" android:layout_marginBottom="8dp" app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginEnd="8dp" app:layout_constraintEnd_toEndOf="parent"
            android:onClick="btnAddChannelNavClicked"/>
    <Button
            android:text="login"
            android:layout_width="wrap_content"
            android:layout_height="32dp"
            android:id="@+id/btnLoginNavHeader" android:layout_marginBottom="8dp"
            app:layout_constraintBottom_toBottomOf="parent" app:layout_constraintStart_toStartOf="parent"
            android:layout_marginStart="8dp" android:background="@android:color/transparent"
            android:onClick="btnLoginNavClicked" android:textColor="@android:color/background_light"
            android:textAlignment="center"/>

</android.support.constraint.ConstraintLayout>
